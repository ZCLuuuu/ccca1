demo-stat.py : simply load as json dict and do stats.
demo-stat_multi_process.py : load json dict with multi processes (with seek()).
demo-strings.py : load file as strings (with seek() and re.search()).
